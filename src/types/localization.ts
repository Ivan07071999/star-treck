export type AppLocalization = {
  title: string;
  navbar: {
    about: string;
    seasons: string;
  };
  buttons: {
    resetCache: string;
    find: string;
  };
  mainPage: {
    title: string;
  };
  placeholders: {
    search: string;
  };
};
