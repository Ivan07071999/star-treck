export type LocaleMessages = {
  title: string;
  navbar: {
    about: string;
    seasons: string;
  };
  buttons: {
    resetCache: string;
    find: string;
  };
  placeholders: {
    search: string;
  };
  about: {
    title: string;
    description: string;
  };
  '404': {
    title: string;
    description: string;
  };
};
